# Base URL configuration (optional)
# NEXT_PUBLIC_APP_URL is recommended for non-Vercel hosting to ensure absolute URLs resolve correctly.
NEXT_PUBLIC_APP_URL="https://your-domain.com"
# On Vercel, VERCEL_URL is injected automatically; no action required.
